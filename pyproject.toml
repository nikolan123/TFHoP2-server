[project]
name = "tfhop2-server"
version = "0.1.0"
description = "Revival server for The Final Hours of Portal 2 online features"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115",
    "jinja2>=3.1",
    "uvicorn[standard]>=0.30",
]
